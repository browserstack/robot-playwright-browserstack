# Local-tunnel variant of browserstack.yml — for testing internal/staging hosts.
# The SDK starts and stops the BrowserStack Local tunnel for you when
# `browserstackLocal: true` is set.
userName: YOUR_USERNAME
accessKey: YOUR_ACCESS_KEY

projectName: BrowserStack Samples
buildName: robot-playwright-sdk-build-1
buildIdentifier: '#${BUILD_NUMBER}'

framework: robot

platforms:
  - os: Windows
    osVersion: 11
    browserName: chrome
    browserVersion: latest

parallelsPerPlatform: 1
browserstackLocal: true

debug: true
networkLogs: false
consoleLogs: errors

source: robot-playwright-sdk:sample-main:v1.0
